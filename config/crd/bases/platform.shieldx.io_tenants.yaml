---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: tenants.platform.shieldx.io
spec:
  group: platform.shieldx.io
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Tenant
            properties:
              isolation:
                type: string
              networkPolicy:
                properties:
                  egress:
                    items:
                      properties:
                        to:
                          description: |-
                            NetworkPolicyPeer is a simplified selector for a peer Pod.
                            NOTE: This is NOT the full Kubernetes NetworkPolicyPeer model.
                            It only supports selecting Pods by labels ("pod"), implicitly within the same namespace.
                          properties:
                            pod:
                              additionalProperties:
                                type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  ingress:
                    items:
                      properties:
                        from:
                          description: |-
                            NetworkPolicyPeer is a simplified selector for a peer Pod.
                            NOTE: This is NOT the full Kubernetes NetworkPolicyPeer model.
                            It only supports selecting Pods by labels ("pod"), implicitly within the same namespace.
                          properties:
                            pod:
                              additionalProperties:
                                type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  podSelector:
                    additionalProperties:
                      type: string
                    type: object
                  policyTypes:
                    items:
                      type: string
                    type: array
                type: object
              owners:
                description: Owners is a list of owner identities (email/OIDC subject/group).
                items:
                  type: string
                minItems: 1
                type: array
              resourceQuota:
                properties:
                  limitsCPU:
                    description: Limits the total amount of CPU resources that can
                      be used by all Pods in a namespace.
                    type: string
                  limitsMemory:
                    description: Limits the total amount of memory resources that
                      can be used by all Pods in a namespace.
                    type: string
                  pods:
                    type: string
                  requestsCPU:
                    description: Limits the total amount of CPU resources that can
                      be requested by all Pods in a namespace.
                    type: string
                  requestsMemory:
                    description: Limits the total amount of memory resources that
                      can be requested by all Pods in a namespace.
                    type: string
                  requestsStorage:
                    type: string
                type: object
              tier:
                type: string
            required:
            - isolation
            - owners
            - tier
            type: object
          status:
            description: status defines the observed state of Tenant
            properties:
              conditions:
                description: Conditions represent the current state of the Tenant
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              namespace:
                description: Namespace is the actual namespace created/managed for
                  this tenant.
                type: string
              phase:
                description: Phase is a simple, high-level summary of the tenant state.
                enum:
                - Pending
                - Ready
                - Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
